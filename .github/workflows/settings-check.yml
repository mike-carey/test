---
name: 'Settings Check'

on:
  pull_request:
    paths:
    - .github/settings.yml

jobs:
  settings_check:
    runs-on: ubuntu-latest
    steps:
    - name: Settings Check
      uses: ghr-actions/settings-check@v0.1.0
