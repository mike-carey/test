---
name: 'Test'

on: [workflow_dispatch]

jobs:
  test:
    name: 'Test'
    runs-on: ubuntu-latest
    steps:
      -
        uses: actions/checkout@v2
      -
        uses: mike-carey/test_repo/actions/test@master
