---
name: 'Test'

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  test:
    name: 'Test'
    runs-on: ubuntu-latest
    environment:
      name: development
    steps:
      - name: 'Attempts to misuse secrets'
        shell: bash
        run: echo "[SHHHH] $MY_SECRET" >&2
